def refine_idea_prompt(idea):
    return f"Refine the following startup idea: {idea}"

def lean_canvas_prompt(idea):
    return f"Generate a Lean Canvas for the startup idea: {idea}"

def mvp_blueprint_prompt(idea):
    return f"Suggest a technical blueprint for the MVP of the startup idea: {idea}"